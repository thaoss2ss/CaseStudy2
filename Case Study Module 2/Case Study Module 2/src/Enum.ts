export enum ChoiceStaff {
    CREATE = 1,
    SHOW,
    UPDATE,
    DELETE,
    FIND,
    SORT,
    SALARY,
    EXIT
}

export enum ChoiceOfSort {
    EXIT,
    AGE,
    NAME,
    POSITION
}

export enum ChoiceOfFind {
    EXIT,
    NAME,
    POSITION,
    DEPARTMENT

}

export enum ChoiceOfSalary {
    EXIT,
    SALARY1 = 1,
    MONEY,


}

export enum ChoiceOfUser {
    SHOW = 1,
    FIND,
    SORT,
    SALARY,
    EXIT,

}

export enum ChoiceOfLogIn {
    SIGNUP = 1,
    SIGNIN,
    EXIT

}
